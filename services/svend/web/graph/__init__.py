"""Graph — Unified Knowledge Graph and Process Model (GRAPH-001).

The persistent, org-wide representation of what an organization knows
about its processes. The graph is topology (nodes + edges). The model
is calibration (Bayesian posteriors on edges from evidence). Synara is
the engine.

Three concerns orbit the graph:
  Graph  — what the organization knows
  Loop   — how the organization learns (LOOP-001)
  QMS    — what the organization must demonstrate (QMS-001)
"""
