# CAL-001: Software Calibration & Verification

**Version:** 1.1
**Status:** Active
**Owner:** claude
**Created:** 2026-03-06
**Last Updated:** 2026-03-06

---

## §1 SCOPE AND PURPOSE

### §1.1 Purpose

CAL-001 treats Svend's codebase as a calibrated measurement device. In metrology, a calibrated device has: known accuracy (traceable to a reference), measurement uncertainty (bounded error), calibration certificates (documented proof), recalibration schedule (periodic re-verification), traceability (chain to national standard), and out-of-tolerance detection (immediate drift alerts). This standard maps those concepts to software quality.

**Core Principle:** If you can't measure it, you can't improve it. If you can't prove it's correct, it isn't.

### §1.2 Scope

Applies to all code under `services/svend/web/`:
- All Python modules (`.py` files)
- All analysis engine functions (`agents_api/dsw/*.py`)
- All view endpoints (`*_views.py`)
- All models (`models.py`)
- All infrastructure (`syn/`, `core/`, `accounts/`)

### §1.3 Metrology Analogy

| Metrology Concept | Software Equivalent | Implementation |
|---|---|---|
| Calibration certificate | Monthly `CalibrationReport` | `generate_calibration_cert` command |
| Measurement uncertainty | Tolerance on golden file assertions | `assertAlmostEqual(delta=)` |
| Traceability | Test → assertion → standard → SOC 2 | `<!-- test: -->` → `<!-- assert: -->` chain |
| Out-of-tolerance (OOT) | Calibration case failure | `DriftViolation` record |
| Recalibration interval | Daily/weekly/monthly/quarterly | Compliance rotation |
| Reference standard | Known-answer dataset | Golden files in `agents_api/tests/golden/` |
| Calibration lab | Compliance runner + test suite | `run_compliance --all` + `manage.py test` |
| NIST traceability | Verified against R/Minitab/textbook | `test_stats_reference.py` |
| Calibration sticker | Dashboard badge (last cert date/result) | Internal dashboard Calibration tab |
| Drift | Previously correct analysis producing wrong output | `DriftViolation` auto-created |

### §1.4 Relationship to Other Standards

| Standard | Relationship |
|---|---|
| TST-001 | Test framework and conventions — CAL-001 extends with coverage measurement and ratchet |
| STAT-001 | Statistical methodology — CAL-001 adds correctness verification via golden files |
| QUAL-001 | Output quality — CAL-001 adds code-level coverage measurement |
| CMP-001 | Compliance automation — CAL-001 adds 3 new checks |
| ARCH-001 | File size limits — CAL-001 enforces via complexity governance check |
| CHG-001 | Change management — all CAL-001 work follows CR lifecycle |

---

## §2 NORMATIVE REFERENCES

- ISO/IEC 17025:2017 (General requirements for testing and calibration laboratories)
- STAT-001 ≥ 1.2 (Statistical Methodology)
- QUAL-001 ≥ 1.0 (Output Quality Assurance)
- TST-001 ≥ 1.4 (Testing Patterns)
- CMP-001 ≥ 1.0 (Compliance Automation)
- ARCH-001 ≥ 1.0 (Architecture & Structure)

---

## §3 TERMINOLOGY

| Term | Definition |
|---|---|
| **Calibration cycle** | A scheduled run of tests + coverage measurement + calibration cases, producing a `CalibrationReport` |
| **Coverage baseline** | The minimum overall line coverage %, stored in `CalibrationReport.ratchet_baseline` |
| **Coverage ratchet** | Mechanism ensuring coverage never decreases — each report becomes the new floor |
| **Golden file** | A JSON file containing known input data, analysis config, and exact expected output values |
| **Measurement uncertainty** | The tolerance band (delta) used in `assertAlmostEqual` for floating-point comparisons |
| **Out-of-tolerance (OOT)** | A calibration case that fails — output differs from expected beyond tolerance |
| **Recalibration schedule** | The frequency at which calibration cycles run (daily: 8 cases, weekly: coverage, monthly: certificate) |
| **Reference standard** | A known-answer dataset with analytically or textbook-proven correct results |
| **Calibration certificate** | A `CalibrationReport` with `is_certificate=True`, generated monthly, containing full system state |
| **Complexity budget** | The maximum lines × cyclomatic complexity product allowed per module |
| **Smoke test** | Minimal test proving an endpoint responds correctly to auth + valid input + invalid input |
| **Module tier** | Risk classification (1-4) determining coverage targets and calibration requirements |

---

## §4 MODULE TIER CLASSIFICATION

Every Python module is assigned a tier based on the risk its failure poses to customer trust and revenue.

<!-- assert: Every source module is classified into exactly one tier (1-4) per CAL-001 §4 | check=cal-tier-classification -->
<!-- impl: syn/audit/compliance.py:check_calibration_coverage -->

### §4.1 Tier 1: Customer Trust (Statistical Correctness)

These modules produce numbers that customers use to make decisions. Wrong math = customer harm.

**Coverage target: 70%**

| Module | Lines | Content | Why Tier 1 |
|---|---|---|---|
| `agents_api/dsw/stats.py` | 19,369 | 102 statistical analyses | Core product — every hypothesis test, regression, ANOVA |
| `agents_api/dsw/spc.py` | 4,742 | 19 SPC analyses | Control charts and capability — quality professionals rely on these daily |
| `agents_api/dsw/bayesian.py` | 4,455 | 23 Bayesian analyses | Bayesian inference — BF₁₀, posteriors, credible intervals |
| `agents_api/dsw/reliability.py` | 1,378 | 10 reliability analyses | Weibull, Kaplan-Meier — used for maintenance planning |
| `agents_api/dsw/dispatch.py` | ~500 | Dispatch routing | Routes to all engines — wrong routing = wrong analysis |
| `agents_api/dsw/standardize.py` | ~400 | Output standardization | Post-processes every result — wrong standardization = wrong narrative |
| `agents_api/dsw/common.py` | 3,022 | Shared utilities | Normality checks, outlier detection, effect sizes — used by all analyses |
| `agents_api/calibration.py` | 688 | Calibration system itself | The system that proves correctness must itself be correct |

### §4.2 Tier 2: Revenue Path (Paying Features + Auth)

These modules handle features customers pay for and the authentication/billing that gates access.

**Coverage target: 50%**

| Module | Lines | Content | Why Tier 2 |
|---|---|---|---|
| `agents_api/dsw/ml.py` | 4,068 | 21 ML analyses | Paid feature — classification, clustering, SHAP |
| `agents_api/dsw_views.py` | 31,112 | DSW HTTP endpoints | API surface for all analysis — auth, routing, response |
| `agents_api/spc_views.py` | 1,328 | SPC endpoints | Control chart API |
| `agents_api/experimenter_views.py` | 2,061 | DOE endpoints | Experiment design API |
| `agents_api/synara_views.py` | 1,129 | Synara endpoints | Belief engine API |
| `accounts/permissions.py` | ~300 | @gated, @gated_paid, @require_auth | Feature gating |
| `accounts/models.py` | ~500 | User, Subscription | Billing models |
| `core/views.py` | 1,815 | Core API | Projects, hypotheses, evidence |
| `agents_api/dsw/viz.py` | 2,688 | 20 visualization types | Chart rendering |
| `agents_api/dsw/simulation.py` | 1,021 | 2 simulation analyses | Monte Carlo, tolerance stackup |

### §4.3 Tier 3: Feature Surface (User-Facing Quality Tools)

These modules provide quality management tools. Bugs degrade user experience but don't produce wrong numbers.

**Coverage target: 30%**

| Module | Lines | Content |
|---|---|---|
| `agents_api/fmea_views.py` | 1,330 | FMEA CRUD + RPN |
| `agents_api/rca_views.py` | 1,060 | RCA sessions + AI critique |
| `agents_api/a3_views.py` | 922 | A3 reports |
| `agents_api/hoshin_views.py` | 1,830 | Hoshin Kanri |
| `agents_api/vsm_views.py` | 854 | Value stream mapping |
| `agents_api/whiteboard_views.py` | 1,041 | Collaborative whiteboards |
| `agents_api/problem_views.py` | 1,161 | Legacy problem management |
| `agents_api/report_views.py` | 787 | CAPA/8D reports |
| `agents_api/iso_views.py` | 2,002 | ISO QMS |
| `agents_api/learn_views.py` | 2,448 | Learning center |
| `agents_api/triage_views.py` | 533 | Data cleaning |
| `agents_api/forecast_views.py` | 363 | Time series forecasting |
| `agents_api/guide_views.py` | 383 | AI decision guide |
| `agents_api/capa_views.py` | 426 | CAPA management |
| `agents_api/autopilot_views.py` | 1,710 | AI autopilot |
| `agents_api/xmatrix_views.py` | 1,025 | X-Matrix |
| `agents_api/workflow_views.py` | 437 | Workflow engine |
| `agents_api/plantsim_views.py` | 387 | Plant simulation |
| `workbench/views.py` | 965 | Unified workbench |
| `forge/views.py` | 422 | Synthetic data |
| `files/views.py` | 420 | File management |
| `chat/views.py` | 34 | Chat |
| `notifications/views.py` | 306 | Notifications |

### §4.4 Tier 4: Infrastructure (Internal Tooling)

Internal tooling and admin. Bugs are operationally annoying but don't affect customers directly.

**Coverage target: 20%**

| Module | Lines | Content |
|---|---|---|
| `syn/audit/compliance.py` | 4,873 | Compliance checks |
| `syn/sched/core.py` | 1,389 | Task scheduler |
| `api/internal_views.py` | 5,389 | Staff dashboard |
| `api/views.py` | 2,019 | Public API (auth, feedback) |
| `syn/audit/models.py` | 1,731 | Audit models |
| `syn/audit/standards.py` | 869 | Standards parser |
| `agents_api/models.py` | 5,078 | Domain models |
| `core/models/*.py` | ~2,000 | Core data models |
| `syn/sched/models.py` | 1,341 | Scheduler models |
| `workbench/models.py` | 1,297 | Workbench models |

---

## §5 CODE COVERAGE MEASUREMENT

### §5.1 Tooling Requirements

<!-- assert: Code coverage measurement configured via .coveragerc with source, omit, and report sections | check=cal-coverage-tooling -->
<!-- impl: .coveragerc -->
<!-- test: syn.audit.tests.test_calibration.CoverageToolingTest.test_coveragerc_exists -->
<!-- test: syn.audit.tests.test_calibration.CoverageToolingTest.test_coverage_importable -->

**Required packages:** `coverage>=7.4`, `pytest-cov>=5.0` (system-wide install via `pip3 install --user`)

**`.coveragerc` specification:**
```ini
[run]
source = .
omit =
    */migrations/*
    */tests/*
    */test_*
    */__pycache__/*
    manage.py
    gunicorn.conf.py
    staticfiles/*
    media/*
    logs/*
    */apps.py
    */admin.py

[report]
exclude_lines =
    pragma: no cover
    def __repr__
    if __name__ == .__main__
    raise NotImplementedError
    pass
    except ImportError
    if TYPE_CHECKING
show_missing = True
precision = 1
fail_under = 0

[html]
directory = htmlcov

[json]
output = coverage.json
```

### §5.2 Coverage Commands

```bash
# Measure coverage
coverage run manage.py test --keepdb
coverage json    # machine-readable for compliance
coverage report  # human-readable summary
coverage html    # visual inspection at htmlcov/index.html
```

### §5.3 Minimum Thresholds (Ratchet)

<!-- assert: Coverage ratchet enforced — new code must not reduce overall coverage percentage | check=cal-coverage-ratchet -->
<!-- impl: syn/audit/compliance.py:check_calibration_coverage -->
<!-- test: syn.audit.tests.test_calibration.CoverageRatchetTest.test_ratchet_enforced -->

Thresholds are aspirational targets approached via ratchet:

| Metric | Phase 1 | Phase 2 | Phase 3 | Phase 4 |
|---|---|---|---|---|
| Overall coverage | Measured | 15% | 30% | 40%+ |
| Tier 1 coverage | Measured | 40% | 60% | 70%+ |
| Tier 2 coverage | Measured | 25% | 40% | 50%+ |
| Tier 3 coverage | Measured | 10% | 20% | 30%+ |
| Tier 4 coverage | Measured | 5% | 10% | 20%+ |

The ratchet mechanism: each `CalibrationReport` stores `ratchet_baseline`. The compliance check `cal-coverage-ratchet` verifies `current_coverage >= ratchet_baseline`. Coverage can only go up.

### §5.4 Coverage Exclusions

Lines excluded from coverage measurement (via `# pragma: no cover`):
- `__repr__` methods (display-only)
- `if __name__ == "__main__"` blocks (CLI entry points)
- `raise NotImplementedError` (abstract methods)
- `except ImportError` (optional dependency handling)
- `if TYPE_CHECKING` (type hint imports)

Everything else counts. No blanket exclusions for "hard to test" code.

---

## §6 STATISTICAL CORRECTNESS VERIFICATION

### §6.1 Golden File Architecture

<!-- assert: Golden files exist for all Tier 1 analyses with deterministic expected outputs — every golden file must have non-empty expected assertions (no empty {} blocks) | check=cal-golden-files -->
<!-- impl: agents_api/tests/golden/ -->
<!-- test: syn.audit.tests.test_calibration.GoldenFileTest.test_golden_files_exist -->
<!-- test: agents_api.tests.test_calibration.CalibrationGoldenTest.test_no_empty_expectations -->

**Directory:** `agents_api/tests/golden/`

**File format:** Each golden file is a JSON document:
```json
{
  "case_id": "GOLD-STATS-TTEST-001",
  "analysis_type": "stats",
  "analysis_id": "ttest",
  "description": "One-sample t-test: known N(100,15) n=50 vs mu=95",
  "input_data": {"x": [101.2, 98.5]},
  "config": {"var1": "x", "mu": 95, "conf": 95},
  "expected": {
    "statistics.p_value": {"value": 0.0312, "tolerance": 0.005},
    "statistics.t_statistic": {"value": 2.21, "tolerance": 0.1},
    "statistics.cohens_d": {"value": 0.333, "tolerance": 0.05}
  },
  "reference": "scipy.stats.ttest_1samp with same data, verified 2026-03-06"
}
```

**Golden file naming:** `{analysis_type}_{analysis_id}_{scenario}.json`

### §6.2 Tolerance Bands

| Statistic Type | Tolerance (delta) | Rationale |
|---|---|---|
| p-value | 0.005 | Sufficient for decision boundary (0.05) |
| Effect size (Cohen's d, η², V) | 0.05 | Within conventional interpretation bands |
| R² | 0.02 | Regression accuracy |
| Confidence interval bounds | 1.0 | Wider tolerance for interval endpoints |
| Bayes Factor (BF₁₀) | 20% of value | Log-scale metric, proportional tolerance |
| Correlation (r) | 0.02 | Tight for directional accuracy |
| Control limits (UCL/LCL) | 0.5 | SPC chart precision |
| Capability indices (Cp/Cpk) | 0.05 | Quality decision boundary (1.33) |

### §6.3 Required Golden Files per Analysis Category

**Priority 1 — Must have before Phase 2 complete:**

| Category | Analyses | Golden Files Each | Total |
|---|---|---|---|
| Stats: Hypothesis testing | ttest, ttest2, paired_t, anova, correlation, chi2, fisher_exact, mann_whitney, kruskal, wilcoxon | 2 (null true + effect present) | 20 |
| Stats: Regression | regression, logistic, stepwise | 1 each | 3 |
| SPC: Control charts | imr, xbar_r, xbar_s, p_chart, cusum, ewma | 2 (stable + OOC) | 12 |
| SPC: Capability | capability | 2 (capable + incapable) | 2 |
| Bayesian | bayes_ttest, bayes_ab, bayes_regression | 1 each | 3 |
| Reliability | weibull, kaplan_meier | 1 each | 2 |
| **Total Priority 1** | | | **42** |

**Priority 2 — Phase 3:**

| Category | Analyses | Golden Files Each | Total |
|---|---|---|---|
| Stats: Post-hoc | tukey_hsd, dunnett, games_howell, dunn | 1 each | 4 |
| Stats: Nonparametric (remaining) | friedman, mood_median, spearman | 1 each | 3 |
| Stats: ANOVA extensions | repeated_measures_anova, nested_anova, anom | 1 each | 3 |
| Stats: MSA | gage_rr, attribute_agreement, icc | 1 each | 3 |
| Stats: Multivariate | hotelling_t2, manova | 1 each | 2 |
| Stats: Time series | arima, decomposition, changepoint | 1 each | 3 |
| Stats: Survival | cox_ph | 1 | 1 |
| ML | classification, regression_ml, clustering, pca | 1 each | 4 |
| Simulation | tolerance_stackup | 1 | 1 |
| **Total Priority 2** | | | **24** |

### §6.4 Cross-Library Verification

<!-- assert: Cross-library verification tests compare Svend output against R/scipy reference values for Tier 1 analyses | check=cal-cross-library -->
<!-- impl: agents_api/tests/test_stats_reference.py -->
<!-- test: syn.audit.tests.test_calibration.CrossLibraryTest.test_reference_tests_exist -->

For the 20 most critical analyses, hardcode reference values computed independently:

| Analysis | Reference Source | Verification |
|---|---|---|
| ttest (1-sample) | `scipy.stats.ttest_1samp` | p-value to 4 decimals |
| ttest2 (2-sample) | `scipy.stats.ttest_ind` | p-value + Cohen's d |
| anova | `scipy.stats.f_oneway` | F-statistic + p-value |
| correlation | `scipy.stats.pearsonr` | r + p-value |
| chi2 | `scipy.stats.chi2_contingency` | χ² + p-value + V |
| mann_whitney | `scipy.stats.mannwhitneyu` | U + p-value |
| regression | `sklearn.linear_model.LinearRegression` | R² + coefficients |
| imr | Minitab reference (textbook) | UCL/LCL + CL |
| capability | Manual calculation | Cp, Cpk to 2 decimals |
| weibull | `scipy.stats.weibull_min.fit` | Shape + scale |

### §6.5 New Analysis Certification

<!-- assert: Every newly registered analysis must have at least one calibration case within 30 days of registration | check=cal-new-analysis-cert -->
<!-- impl: syn/audit/compliance.py:check_calibration_coverage -->

When a new `(analysis_type, analysis_id)` pair is added to `ANALYSIS_REGISTRY`:
1. It must have a calibration case within 30 days
2. The calibration case must pass
3. Until certified, the compliance check flags it as WARNING

---

## §7 ENDPOINT COVERAGE

### §7.1 Endpoint Smoke Test Requirements

<!-- assert: Every public view function has at least one smoke test covering auth + valid input + invalid input | check=cal-endpoint-smoke -->
<!-- impl: syn/audit/compliance.py:check_endpoint_coverage -->
<!-- test: syn.audit.tests.test_calibration.EndpointSmokeTest.test_all_endpoints_have_smoke -->

Every view function in every `*_views.py` file requires at minimum:
- Auth required test (unauthenticated returns 401/403)
- Valid input test (responds without 500)
- Invalid input test (returns 400/422, not 500)

### §7.2 Endpoint Inventory by Module

**Total: 600+ endpoints across 32 view files.**

| View File | Endpoints | Existing Tests | Gap |
|---|---|---|---|
| `dsw_views.py` | 26 | ~5 | 21 |
| `synara_views.py` | 28 | 6 | 22 |
| `spc_views.py` | 8 | ~2 | 6 |
| `experimenter_views.py` | 9 | ~1 | 8 |
| `problem_views.py` | 16 | ~3 | 13 |
| `a3_views.py` | 13 | ~2 | 11 |
| `rca_views.py` | 16 | ~2 | 14 |
| `fmea_views.py` | 20 | ~3 | 17 |
| `hoshin_views.py` | 32 | 49 | ~0 |
| `vsm_views.py` | 12 | 47 | ~0 |
| `whiteboard_views.py` | 17 | 83 | ~0 |
| `triage_views.py` | 6 | ~1 | 5 |
| `forecast_views.py` | 2 | 0 | 2 |
| `learn_views.py` | 11 | 0 | 11 |
| `guide_views.py` | 3 | 0 | 3 |
| `iso_views.py` | 32 | 218 | ~0 |
| `autopilot_views.py` | 4 | 0 | 4 |
| `report_views.py` | 11 | ~1 | 10 |
| `capa_views.py` | 4 | 0 | 4 |
| `action_views.py` | 2 | 0 | 2 |
| `workflow_views.py` | 3 | 0 | 3 |
| `xmatrix_views.py` | 10 | 0 | 10 |
| `plantsim_views.py` | 8 | 0 | 8 |
| `token_views.py` | 1 | 0 | 1 |
| `api/views.py` | 26 | 79 | ~0 |
| `api/internal_views.py` | 80+ | 0 | 80 |
| `core/views.py` | 35 | 70 | ~0 |
| `workbench/views.py` | 38 | 22 | 16 |
| `forge/views.py` | 8 | 23 | ~0 |
| `files/views.py` | 9 | 0 | 9 |
| `chat/views.py` | 1 | 14 | ~0 |
| `notifications/views.py` | 7 | 57 | ~0 |

**Total gap: ~270 endpoints without any test.**

---

## §8 COMPLEXITY GOVERNANCE

### §8.1 File Size Limits (from ARCH-001 §7)

<!-- assert: No Python file exceeds 3000 lines without approved exemption in DEBT.md | check=cal-complexity-budget -->
<!-- impl: syn/audit/compliance.py:check_complexity_governance -->
<!-- test: syn.audit.tests.test_complexity_governance.ExemptionAccuracyTest.test_no_unexempted_oversize_files -->
<!-- test: syn.audit.tests.test_complexity_governance.ExemptionAccuracyTest.test_all_exemptions_still_needed -->
<!-- test: syn.audit.tests.test_complexity_governance.ExemptionAccuracyTest.test_exemption_count_matches_oversized_count -->

| Threshold | Action |
|---|---|
| > 2000 lines | WARNING — consider splitting |
| > 3000 lines | FAIL unless exempted in `_COMPLEXITY_EXEMPTIONS` |

**Current exempted files (>3000 lines, updated 2026-03-06):**

| File | Lines | Priority | Notes |
|---|---|---|---|
| `agents_api/learn_content.py` | 6,511 | P3 | Content data — static course material |
| `api/internal_views.py` | 5,511 | P3 | Admin dashboard — many small views |
| `agents_api/dsw/stats_exploratory.py` | 5,385 | P3 | Statistical engine — tightly coupled |
| `syn/audit/compliance.py` | 5,255 | P3 | Compliance engine — 28 checks |
| `agents_api/models.py` | 5,078 | P3 | Domain models — split into package |
| `agents_api/dsw/spc.py` | 4,742 | P3 | SPC engine — tightly coupled |
| `agents_api/dsw/bayesian.py` | 4,455 | P3 | Bayesian engine — tightly coupled |
| `agents_api/dsw/stats_advanced.py` | 4,407 | P3 | Statistical engine — tightly coupled |
| `agents_api/dsw/ml.py` | 4,068 | P3 | ML engine — tightly coupled |
| `agents_api/pbs_engine.py` | 3,796 | P3 | PBS engine — tightly coupled |
| `agents_api/dsw/common.py` | 3,022 | P3 | Shared utilities — marginal overage |

**Historical:** `dsw_views.py` (31K) was split into the `dsw/` package (ARCH-001 CR). `stats.py` (19K) was split into 7 sub-modules (stats_parametric, stats_exploratory, stats_advanced, stats_regression, etc.).

### §8.2 Warning-Level Files (2000-3000 lines)

These files are above the warning threshold but below the failure limit. No exemption needed — they generate compliance warnings to track growth.

| File | Lines | Notes |
|---|---|---|
| `agents_api/dsw/stats_regression.py` | 2,906 | Approaching 3000 |
| `agents_api/dsw/d_type.py` | 2,780 | Data type handlers |
| `agents_api/dsw/viz.py` | 2,675 | Visualization engine |
| `agents_api/dsw/education_advanced.py` | 2,563 | Education content |
| `agents_api/dsw/stats_parametric.py` | 2,479 | Statistical tests |
| `agents_api/learn_views.py` | 2,448 | Learning center views |
| `agents_api/dsw/education.py` | 2,443 | Education content |
| `agents_api/dsw/endpoints_data.py` | 2,107 | Endpoint registry |
| `agents_api/experimenter_views.py` | 2,061 | DOE endpoints |
| `api/views.py` | 2,019 | API views |
| `agents_api/iso_views.py` | 2,002 | ISO compliance views |

---

## §9 REGRESSION PREVENTION

### §9.1 No-Regression Rule

<!-- assert: Coverage percentage never decreases between calibration cycles | check=cal-no-regression -->
<!-- impl: syn/audit/models.py:CalibrationReport -->
<!-- test: syn.audit.tests.test_calibration.NoRegressionTest.test_ratchet_enforced -->

Once a `CalibrationReport` records a coverage percentage, that becomes the floor. The `check_calibration_coverage` compliance check compares current coverage against the stored baseline. If current < baseline, the check FAILs.

### §9.2 Golden File Immutability

Golden files must not be modified to make failing tests pass. If a golden file test fails:
1. Investigate whether the analysis function changed
2. If the analysis was improved (corrected), update the golden file AND document the change in a CHG-001 CR
3. If the analysis regressed (got worse), fix the analysis, not the golden file
4. Every golden file modification must be justified in the commit message

---

## §10 CALIBRATION CYCLES

### §10.1 Daily Calibration (Automated)

<!-- assert: Daily calibration runs 8 random cases via existing statistical_calibration compliance check | check=cal-daily-cycle -->
<!-- impl: syn/audit/compliance.py:check_statistical_calibration -->

Already implemented. Runs 8 of N cases daily, date-seeded for reproducibility. Creates `DriftViolation` for failures.

### §10.2 Weekly Coverage Audit

<!-- assert: Weekly coverage measurement stores CalibrationReport with overall and per-tier coverage | check=cal-weekly-audit -->
<!-- impl: syn/audit/management/commands/measure_coverage.py -->

```bash
coverage run manage.py test --keepdb && coverage json
python3 manage.py measure_coverage
```

The `measure_coverage` command:
1. Reads `coverage.json`
2. Computes overall coverage and per-tier coverage
3. Creates `CalibrationReport` with metrics
4. Compares against ratchet baseline
5. Outputs pass/fail to stdout

### §10.3 Monthly Calibration Certificate

<!-- assert: Monthly calibration certificate generated with pass/fail determination | check=cal-monthly-cert -->
<!-- impl: syn/audit/management/commands/generate_calibration_cert.py -->
<!-- test: syn.audit.tests.test_calibration.MonthlyCertTest.test_certificate_generated -->

```bash
python3 manage.py generate_calibration_cert
```

Produces a `CalibrationReport` with `is_certificate=True`:
- Runs ALL calibration cases (not just 8)
- Runs full coverage measurement
- Checks all 3 CAL-001 compliance checks
- Stores complete per-module breakdown in `details` JSON
- Reports pass/fail determination

---

## §11 CALIBRATION REPORTS

### §11.1 CalibrationReport Model

```python
class CalibrationReport(models.Model):
    """Point-in-time calibration snapshot."""
    date = models.DateField()
    overall_coverage = models.FloatField(null=True)
    tier1_coverage = models.FloatField(null=True)
    tier2_coverage = models.FloatField(null=True)
    tier3_coverage = models.FloatField(null=True)
    tier4_coverage = models.FloatField(null=True)
    calibration_pass_rate = models.FloatField(null=True)
    calibration_cases_run = models.IntegerField(default=0)
    calibration_cases_passed = models.IntegerField(default=0)
    endpoint_coverage = models.FloatField(null=True)
    golden_file_count = models.IntegerField(default=0)
    complexity_violations = models.IntegerField(default=0)
    ratchet_baseline = models.FloatField(default=0.0)
    is_certificate = models.BooleanField(default=False)
    details = models.JSONField(default=dict)
```

### §11.2 Dashboard Integration

Add "Calibration" tab to `/internal/dashboard/` showing:
- Coverage trend line (monthly, with ratchet floor)
- Calibration pass rate (30-day rolling)
- Module heatmap (per-module coverage, colored by tier)
- Complexity chart (largest files with 3000-line limit)
- Golden file count growth
- Last certificate date and pass/fail badge

---

## §12 ANTI-PATTERNS

### §12.1 Coverage Gaming

**PROHIBITED:** Writing tests that execute code without verifying behavior, solely to increase coverage numbers.

### §12.2 Tolerance Widening

**PROHIBITED:** Widening golden file tolerances to make failing tests pass. If a test fails within its tolerance band, the analysis function has a bug.

### §12.3 Skipping Calibration

**PROHIBITED:** Disabling, skipping, or reducing calibration cycles. The daily 8-case rotation is the minimum.

## §13 METRIC DEFINITIONS AND CONSISTENCY

### §13.1 Dashboard Metrics Registry

Every number displayed on the internal dashboard Calibration tab is defined here. No metric may appear on the dashboard without a corresponding entry in this section.

<!-- assert: Every calibration dashboard metric has a formal definition in CAL-001 §13 with source, formula, and consistency invariant | check=cal-metric-definitions -->
<!-- impl: api/internal_views.py:api_calibration -->

#### M1: Line Coverage (Overall)

| Property | Value |
|---|---|
| **Dashboard element** | `#cal-coverage` KPI card |
| **Label** | "Line Coverage" |
| **Source** | `CalibrationReport.overall_coverage` (latest by date) |
| **Upstream data** | `coverage.json` → `totals.percent_covered` |
| **Produced by** | `python manage.py measure_coverage` (reads `coverage.json`) |
| **Prerequisite** | `coverage run manage.py test --keepdb && coverage json` |
| **Formula** | `(covered_lines / num_statements) × 100` from pytest-cov |
| **Unit** | Percent (0-100) |
| **What it measures** | % of Python statements executed during the test suite |
| **What it does NOT measure** | Correctness of outputs, symbol governance, calibration case coverage |

#### M2: Tier Coverage (T1-T4)

| Property | Value |
|---|---|
| **Dashboard elements** | `#cal-t1`, `#cal-t2`, `#cal-t3`, `#cal-t4` KPI cards |
| **Labels** | "Tier 1 (Trust) / 70%", "Tier 2 (Revenue) / 50%", "Tier 3 (Feature) / 30%", "Tier 4 (Infra) / 20%" |
| **Source** | `CalibrationReport.tier1_coverage` through `tier4_coverage` |
| **Upstream data** | `coverage.json` → per-file `summary.covered_lines / summary.num_statements` |
| **Produced by** | `_tier_coverage()` in `measure_coverage.py`, using `TIER1_MODULES` through `TIER4_MODULES` |
| **Formula** | `sum(covered_lines for tier modules) / sum(num_statements for tier modules) × 100` |
| **Unit** | Percent (0-100) |
| **What it measures** | Line coverage scoped to the module list for that tier |
| **Module lists** | Defined in `measure_coverage.py` — must match §4 tier tables exactly |

#### M3: Line Coverage Trend

| Property | Value |
|---|---|
| **Dashboard element** | `#chart-cal-trend` canvas (line chart) |
| **Label** | "Line Coverage Trend" |
| **Source** | All `CalibrationReport` records with `overall_coverage IS NOT NULL`, ordered by date |
| **Datasets** | Coverage line (`overall_coverage`) + Ratchet floor line (`ratchet_baseline`) |
| **What it measures** | Historical progression of M1 over time |

#### M4: Ratchet Floor

| Property | Value |
|---|---|
| **Dashboard element** | `#cal-ratchet` KPI card |
| **Label** | "Ratchet Floor" |
| **Source** | `CalibrationReport.ratchet_baseline` (latest by date) |
| **Formula** | `max(current_overall_coverage, previous_ratchet_baseline)` |
| **Invariant** | Monotonically non-decreasing across reports. `ratchet(report_N) >= ratchet(report_N-1)` |
| **What it measures** | The minimum acceptable line coverage — can only go up |

#### M5: Calibration Pass Rate

| Property | Value |
|---|---|
| **Dashboard element** | `#cal-pass-rate` KPI card |
| **Label** | "Calibration Pass" |
| **Source** | `CalibrationReport.calibration_pass_rate` (latest report, preferring latest certificate) |
| **Upstream data** | `run_calibration()` in `agents_api/calibration.py` |
| **Formula** | `(cases_passed / cases_run) × 100` |
| **Unit** | Percent (0-100) |
| **What it measures** | % of reference calibration cases where the analysis function produces output within tolerance of known-correct values |
| **What it does NOT measure** | Code coverage, symbol governance, endpoint coverage |

#### M6: Golden File Count

| Property | Value |
|---|---|
| **Dashboard element** | `#cal-golden` KPI card |
| **Label** | "Golden Files" |
| **Source** | `CalibrationReport.golden_file_count` |
| **Formula** | `count(*.json files in agents_api/tests/golden/)` |
| **What it measures** | Number of known-answer reference datasets available for calibration |

#### M7: Complexity Violations

| Property | Value |
|---|---|
| **Dashboard element** | `#cal-complexity` KPI card |
| **Label** | "Complexity Issues" |
| **Source** | `CalibrationReport.complexity_violations` |
| **Formula** | Count of `.py` files > 3000 lines (excluding migrations and tests) |
| **What it measures** | Files exceeding the complexity budget per §8 |

#### M8: Symbol Governance (Compliance Tab — NOT Calibration)

| Property | Value |
|---|---|
| **Dashboard element** | `#compliance-coverage` KPI card (on Compliance tab, not Calibration) |
| **Label** | "Symbol Governance" |
| **Source** | `check_symbol_coverage()` in `syn/audit/compliance.py` |
| **Formula** | `(symbols with both impl AND test hooks) / (total public symbols via AST) × 100` |
| **Unit** | Percent (0-100) |
| **What it measures** | % of public functions/classes referenced by `<!-- impl: file:Symbol -->` hooks in standards where that assertion also has `<!-- test: -->` hooks |
| **What it does NOT measure** | Whether the code runs correctly, line coverage, calibration case coverage |

### §13.2 Metric Consistency Invariants

These invariants must hold at all times. The compliance check `cal-metric-consistency` verifies them.

<!-- assert: Dashboard metric consistency invariants verified — no two metrics with same label show different values | check=cal-metric-consistency -->
<!-- impl: syn/audit/compliance.py:check_calibration_coverage -->

| ID | Invariant | Verification |
|---|---|---|
| C1 | `cal-coverage` card value = latest `CalibrationReport.overall_coverage` | Card reads from `stats.overall_coverage` in API response |
| C2 | Chart latest data point = `cal-coverage` card value | Both read same field; chart deduplicates to latest-per-date |
| C3 | Tier values sum-weighted ≈ overall (within 5%) | `_tier_coverage()` modules must cover the same files as `.coveragerc` source |
| C4 | `ratchet_baseline` is monotonically non-decreasing | `max(current, previous)` — verified by `cal-coverage-ratchet` |
| C5 | `calibration_pass_rate` ∈ [0, 100] | Bounded by `cases_passed <= cases_run` |
| C6 | `golden_file_count` = actual count of `agents_api/tests/golden/*.json` | Re-counted each report; stale values indicate skipped measurement |
| C7 | "Line Coverage" label appears ONLY on Calibration tab | Symbol Governance appears ONLY on Compliance tab |
| C8 | No two KPI cards on same tab share identical label text | Prevents the confusion that prompted this section |

### §13.3 Metric Sources Summary

| Tab | Metric | Source | Command |
|---|---|---|---|
| Calibration | Line Coverage (M1) | `coverage.json` → pytest-cov | `coverage run ... && coverage json && manage.py measure_coverage` |
| Calibration | Tier 1-4 (M2) | `coverage.json` per tier module list | same as M1 |
| Calibration | Trend (M3) | Historical `CalibrationReport.overall_coverage` | same as M1 |
| Calibration | Ratchet (M4) | `CalibrationReport.ratchet_baseline` | same as M1 |
| Calibration | Pass Rate (M5) | `run_calibration()` → reference case results | `manage.py generate_calibration_cert` or daily compliance |
| Calibration | Golden Files (M6) | `agents_api/tests/golden/*.json` file count | same as M1 |
| Calibration | Complexity (M7) | `.py` file line count scan | same as M1 |
| Compliance | Symbol Governance (M8) | AST symbol walk + standards hook parsing | `manage.py run_compliance --check symbol_coverage` |

### §13.4 Calibration Case Coverage (Future Metric)

The calibration system tracks 62 reference cases against 230 registered DSW analyses (26.5% coverage). This is tracked in the inventory below but not yet surfaced as a dashboard metric.

| Category | Registered | Calibrated | Coverage |
|---|---|---|---|
| stats | 106 | 32 | 30.2% |
| spc | 22 | 11 | 50.0% |
| bayesian | 22 | 7 | 31.8% |
| ml | 21 | 6 | 28.6% |
| reliability | 10 | 4 | 40.0% |
| simulation | 2 | 2 | 100.0% |
| viz | 21 | 0 | 0.0% |
| pbs | 9 | 0 | 0.0% |
| d_type | 6 | 0 | 0.0% |
| other | 11 | 0 | 0.0% |
| **Total** | **230** | **62** | **26.5%** |

When this metric is added to the dashboard, it must be labeled distinctly (e.g., "Calibration Coverage") and defined here as M9 with source `calibration.py` case registry vs `registry.py` analysis registry.

---

## ANALYSIS INVENTORY — Complete (230 Registered, 62 Calibrated)

### Stats Module (102 analyses)

**Hypothesis Testing (18):** ttest, ttest2, paired_t, anova, anova2, correlation, chi2, fisher_exact, poisson_1sample, poisson_2sample, variance_test, f_test, equivalence, runs_test, meta_analysis, sprt, normality, sign_test

**Regression (11):** regression, logistic, nominal_logistic, ordinal_logistic, orthogonal_regression, nonlinear_regression, poisson_regression, robust_regression, stepwise, best_subsets, glm

**Nonparametric (6):** mann_whitney, kruskal, wilcoxon, friedman, spearman, mood_median

**ANOVA Extensions (7):** split_plot_anova, repeated_measures_anova, nested_anova, anom, main_effects, interaction, variance_components

**Post-Hoc (7):** tukey_hsd, dunnett, games_howell, dunn, scheffe_test, bonferroni_test, hsu_mcb

**Power & Sample Size (9):** power_z, power_1prop, power_2prop, power_1variance, power_2variance, power_equivalence, power_doe, sample_size_ci, sample_size_tolerance

**Time Series (7):** arima, sarima, decomposition, acf_pacf, granger, changepoint, ccf

**Survival (3):** weibull, kaplan_meier, cox_ph

**MSA (10):** gage_rr, gage_rr_nested, gage_rr_expanded, gage_linearity_bias, gage_type1, attribute_gage, attribute_agreement, icc, krippendorff_alpha, bland_altman

**Multivariate (3):** hotelling_t2, manova, multi_vari

**Exploratory (14):** descriptive, data_profile, auto_profile, graphical_summary, missing_data_analysis, outlier_analysis, duplicate_analysis, bootstrap_ci, box_cox, johnson_transform, run_chart, grubbs_test, tolerance_interval, effect_size_calculator

**Acceptance Sampling (3):** acceptance_sampling, variable_acceptance_sampling, multiple_plan_comparison

**Capability (3):** capability_sixpack, nonnormal_capability_np, attribute_capability

**Distribution (4):** distribution_fit, mixture_model, copula, prop_1sample, prop_2sample

### SPC Module (22 analyses)

imr, xbar_r, xbar_s, p_chart, np_chart, c_chart, u_chart, cusum, ewma, laney_p, laney_u, moving_average, zone_chart, mewma, generalized_variance, capability, nonnormal_capability, between_within, conformal_control, conformal_monitor, entropy_spc, degradation_capability

### ML Module (21 analyses)

classification, regression_ml, model_compare, xgboost, lightgbm, shap_explain, hyperparameter_tune, clustering, pca, feature, bayesian_regression, gam, isolation_forest, gaussian_process, pls, sem, regularized_regression, discriminant_analysis, factor_analysis, correspondence_analysis, item_analysis

### Bayesian Module (23 analyses)

bayes_regression, bayes_ttest, bayes_ab, bayes_correlation, bayes_anova, bayes_changepoint, bayes_proportion, bayes_capability_prediction, bayes_equivalence, bayes_chi2, bayes_poisson, bayes_logistic, bayes_survival, bayes_meta, bayes_demo, bayes_spares, bayes_system, bayes_warranty, bayes_repairable, bayes_rul, bayes_alt, bayes_comprisk

### Reliability Module (10 analyses)

weibull, lognormal, exponential, kaplan_meier, reliability_test_plan, distribution_id, accelerated_life, repairable_systems, warranty, competing_risks

### Other Modules (55 analyses)

**Viz (20):** histogram, boxplot, dotplot, individual_value_plot, probability, scatter, interval_plot, bubble, mosaic, heatmap, matrix, parallel_coordinates, pareto, contour, surface_3d, contour_overlay, timeseries, bayes_spc_capability, bayes_spc_changepoint, bayes_spc_control

**D-Type (6):** d_chart, d_cpk, d_nonnorm, d_equiv, d_sig, d_multi

**PBS (9):** pbs_full, pbs_belief, pbs_edetector, pbs_evidence, pbs_predictive, pbs_adaptive, pbs_cpk, pbs_cpk_traj, pbs_health

**Quality Economics (4):** taguchi_loss, process_decision, lot_sentencing, cost_of_quality

**Causal (2):** causal_pc, causal_lingam

**Simulation (2):** tolerance_stackup, variance_propagation

**Drift (1):** drift_report

**Anytime (2):** anytime_ab, anytime_onesample

**Bayes MSA (1):** bayes_msa

**ISHAP (1):** ishap

### Calibration Case Summary (83 cases, 62 unique analyses)

Source: `agents_api/calibration.py` — authoritative registry of all calibration cases.

| Category | Cases | Unique Analyses | Registered | Coverage |
|---|---|---|---|---|
| stats | 43 | 32 | 106 | 30.2% |
| spc | 18 | 11 | 22 | 50.0% |
| bayesian | 9 | 7 | 22 | 31.8% |
| ml | 6 | 6 | 21 | 28.6% |
| reliability | 5 | 4 | 10 | 40.0% |
| simulation | 2 | 2 | 2 | 100.0% |
| viz | 0 | 0 | 21 | 0.0% |
| pbs | 0 | 0 | 9 | 0.0% |
| d_type | 0 | 0 | 6 | 0.0% |
| other | 0 | 0 | 11 | 0.0% |
| **Total** | **83** | **62** | **230** | **26.5%** |

Multiple cases per analysis cover different scenarios (e.g., CAL-INF-003/004 = anova null + effect, CAL-SPC-001/002 = imr stable + shift).

**Gap: 168 registered analyses without calibration cases.** Priority zero-coverage categories: viz (21), pbs (9), d_type (6).

---

## Revision History

| Version | Date | Author | Changes |
|---|---|---|---|
| 1.0 | 2026-03-06 | claude | Initial release — Phase 1 infrastructure |
| 1.1 | 2026-03-06 | claude | §13 Metric Definitions — formal definition of all dashboard metrics with sources, formulas, consistency invariants. Updated calibration inventory from 16 to 83 cases (62 unique analyses). CR: 501cff02 |
